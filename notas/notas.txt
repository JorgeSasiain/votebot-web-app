Tecnologías
- Webpack
- Babel (compilador JS)
- http-server
- React
- react-router
- react-select
- strophe.js
- node-xmpp-bosh

Estructura
- package.json
- webpack.config.js
- src
-- index.js (punto de entrada)
-- components
--- Routes.js (Rutas de la app)
--- Layout.js (componente principal que aplica a todas las páginas)
--- IndexPage.js (página principal, carga un componente u otro en función de usuario loggeado o no)
--- >XmppLogin.js (componente para hacer login XMPP)
--- >MainMenu.js (componente donde se muestran las opciones tras hacer login)
--- >>NewPoll.js (componente donde un usuario puede enviar una nueva encuesta)
--- >>ManagePolls.js (componente donde un usuario puede gestionar sus encuestas activas)
-- xmpp
--- xmpp.js (funcionalidades XMPP utilizando strophe.js)
-- static
--- index.html
--- css
--- js (donde webpack genera bundle.js)

//decubrir mucs asociadas al usuario con iq get disco_items al servidor del usuario y mostrarlas
//descubrir usuarios en cada muc con iq get disco_items al muc, y mostrarlos al lado de cada muc si el usuario pulsa un boton
//enviar encuesta a las mucs que el usuario seleccione

//descubrir grupos del usuario con iq get roster (igual que hacia para contactos pero buscar <group> en cada <item>)
//descubrir contactos que pertenecen a cada grupo, y mostrarlos al lado de cada grupo si el usuaio pulsa un boton
//enviar encuesta a los usuarios de cada grupo seleccionado (sin repetir usuarios)

//descubrir todos los contactos en la pag web y mostrarlos a eleccion del usuario mediante un boton (ej. elegir solo mostrar de un tipo de suscripcion o todos)
//si el usuario decide mostrarlos, permitirle seleccionar los que quiera, y enviar encuesta a esos (sin repetir usuarios)

//XEP
//0124 (BOSH) (ya estaba)
//0045 (mucs)
//0030 (service discovery)